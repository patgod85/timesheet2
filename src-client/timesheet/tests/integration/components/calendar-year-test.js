import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('calendar-year', 'Integration | Component | calendar year', {
  integration: true
});

test('it renders', function(assert) {
  assert.equal('', '');
});
