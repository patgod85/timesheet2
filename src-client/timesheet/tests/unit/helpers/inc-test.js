
import { inc } from 'timesheet2/helpers/inc';
import { module, test } from 'qunit';

module('Unit | Helper | inc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inc([42]);
  assert.ok(result);
});

