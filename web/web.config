<?xml version="1.0" encoding="utf-8"?>
<!--This file is autogenerated and can be rewritten-->
<configuration>
    <system.webServer>
        <rewrite>
            <rules>
                <rule name="For .local call app_dev.php"
                      stopProcessing="true">
                    <match url="^([^\?]+)\??([^/]*)?$" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{PATH_TRANSLATED}"
                             matchType="IsFile"
                             negate="true" />
                        <add input="{DOCUMENT_ROOT}{PATH_INFO}index.php"
                             matchType="IsFile"
                             negate="true" />
                        <add input="{HTTP_HOST}"
                             pattern="^.+\.local$"
                             ignoreCase="false" />
                    </conditions>
                    <action type="Rewrite"
                            url="/app_dev.php/{R:1}?{R:2}"
                            appendQueryString="true" />
                </rule>
                <rule name="For any call app.php"
                      stopProcessing="true">
                    <match url="^([^\?]+)\??([^/]*)?$" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{PATH_TRANSLATED}"
                             matchType="IsFile"
                             ignoreCase="false"
                             negate="true" />
                        <add input="{DOCUMENT_ROOT}{PATH_INFO}index.php"
                             matchType="IsFile"
                             negate="true" />
                    </conditions>
                    <action type="Rewrite"
                            url="/app.php/{R:1}?{R:2}"
                            appendQueryString="true" />
                </rule>
            </rules>
        </rewrite>
    </system.webServer>
</configuration>
